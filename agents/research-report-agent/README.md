# AI Research/Report Agent

This folder contains the standalone demo logic for the AI Research/Report Agent shown on the AgentMart storefront.

- `agent.mjs` — runnable JavaScript module with the agent workflow logic.
- The storefront also wires these same behaviors into `app.js` for in-browser demos.

These modules are frontend-safe demos designed to prove workflow behavior before connecting real production APIs.
